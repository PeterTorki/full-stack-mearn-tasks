import { ToggleLogin } from './toggle-login';

describe('ToggleLogin', () => {
  it('should create an instance', () => {
    const directive = new ToggleLogin();
    expect(directive).toBeTruthy();
  });
});
